import pandas as pd
import numpy as np

def generate_data():
    data = pd.DataFrame(np.random.randn(100, 3), columns=['A', 'B', 'C'])
    return data

def process_data(data):
    rolling_mean = data.rolling(window=5).mean()
    return rolling_mean

def main():
    data = generate_data()
    processed_data = process_data(data)
    # No output happens after this

if __name__ == "__main__":
    main()
